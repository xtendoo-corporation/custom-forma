<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Plantilla preparada para futuras personalizaciones del layout
        De momento no se aplica ningún cambio
    -->
    <template id="external_layout_folder_inherit" inherit_id="web.external_layout_folder">
        <!-- Las personalizaciones del layout se agregarán aquí cuando sean necesarias -->

        <!-- Ejemplo: Personalización del header (comentado)
        <xpath expr="//div[contains(@t-attf-class, 'header o_company_')]" position="replace">
            <div t-attf-class="header o_company_#{company.id}_layout" t-att-style="report_header_style">
                ...
            </div>
        </xpath>
        -->
    </template>
</odoo>
